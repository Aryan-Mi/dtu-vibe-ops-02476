name: Build and Push Docker Images to Artifact Registry

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  PROJECT_ID: ${{ vars.GCP_PROJECT_ID || 'vibeops-483814' }}
  REGION: ${{ vars.GCP_REGION || 'europe-west1' }}
  ARTIFACT_REGISTRY: ${{ vars.GCP_ARTIFACT_REGISTRY || 'dtu-vibe-ops' }}

jobs:
  build-train:
    name: Build and Push Train and API Docker Images
    runs-on: ubuntu-latest
    environment: production
    permissions:
      contents: read
      id-token: write
    outputs:
      train-image-uri-sha: ${{ steps.train-image-info.outputs.image-uri-sha }}
      train-image-uri-latest: ${{ steps.train-image-info.outputs.image-uri-latest }}
      train-commit-sha: ${{ github.sha }}
      train-image-size: ${{ steps.train-image-info.outputs.image-size }}
      api-image-uri-sha: ${{ steps.api-image-info.outputs.image-uri-sha }}
      api-image-uri-latest: ${{ steps.api-image-info.outputs.image-uri-latest }}
      api-image-size: ${{ steps.api-image-info.outputs.image-size }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}
          create_credentials_file: true
          export_environment_variables: true

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: |
          gcloud auth configure-docker ${REGION}-docker.pkg.dev

      - name: Build train image
        id: build-train
        run: |
          export IMAGE_TAG=${REGION}-docker.pkg.dev/${PROJECT_ID}/${ARTIFACT_REGISTRY}/dtu-vibe-ops-02476-train
          docker build . --file dockerfiles/train.dockerfile \
            --platform linux/amd64 \
            --tag ${IMAGE_TAG}:${{ github.sha }} \
            --tag ${IMAGE_TAG}:latest

          # Get image size
          IMAGE_SIZE=$(docker images ${IMAGE_TAG}:latest --format "{{.Size}}")
          echo "image-size=${IMAGE_SIZE}" >> $GITHUB_OUTPUT

      - name: Push train image to Artifact Registry
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          export IMAGE_TAG=${REGION}-docker.pkg.dev/${PROJECT_ID}/${ARTIFACT_REGISTRY}/dtu-vibe-ops-02476-train
          docker push ${IMAGE_TAG}:${{ github.sha }}
          docker push ${IMAGE_TAG}:latest

      - name: Set image info outputs
        id: train-image-info
        run: |
          IMAGE_TAG_BASE="${REGION}-docker.pkg.dev/${PROJECT_ID}/${ARTIFACT_REGISTRY}/dtu-vibe-ops-02476-train"
          echo "image-uri-sha=${IMAGE_TAG_BASE}:${{ github.sha }}" >> $GITHUB_OUTPUT
          echo "image-uri-latest=${IMAGE_TAG_BASE}:latest" >> $GITHUB_OUTPUT
          echo "image-size=${{ steps.build-train.outputs.image-size }}" >> $GITHUB_OUTPUT

      - name: Build API image
        id: build-api
        run: |
          export IMAGE_TAG=${REGION}-docker.pkg.dev/${PROJECT_ID}/${ARTIFACT_REGISTRY}/dtu-vibe-ops-02476-api
          docker build . --file dockerfiles/api.dockerfile \
            --platform linux/amd64 \
            --tag ${IMAGE_TAG}:${{ github.sha }} \
            --tag ${IMAGE_TAG}:latest

          # Get image size
          IMAGE_SIZE=$(docker images ${IMAGE_TAG}:latest --format "{{.Size}}")
          echo "image-size=${IMAGE_SIZE}" >> $GITHUB_OUTPUT

      - name: Push API image to Artifact Registry
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          export IMAGE_TAG=${REGION}-docker.pkg.dev/${PROJECT_ID}/${ARTIFACT_REGISTRY}/dtu-vibe-ops-02476-api
          docker push ${IMAGE_TAG}:${{ github.sha }}
          docker push ${IMAGE_TAG}:latest

      - name: Set API image info outputs
        id: api-image-info
        run: |
          IMAGE_TAG_BASE="${REGION}-docker.pkg.dev/${PROJECT_ID}/${ARTIFACT_REGISTRY}/dtu-vibe-ops-02476-api"
          echo "image-uri-sha=${IMAGE_TAG_BASE}:${{ github.sha }}" >> $GITHUB_OUTPUT
          echo "image-uri-latest=${IMAGE_TAG_BASE}:latest" >> $GITHUB_OUTPUT
          echo "image-size=${{ steps.build-api.outputs.image-size }}" >> $GITHUB_OUTPUT

      - name: Build Summary (PR)
        if: github.event_name == 'pull_request'
        run: |
          echo "## ✅ Training and API Image Build Validation" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Training Image" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ **Build successful** - Image validated but not pushed to registry" >> $GITHUB_STEP_SUMMARY
          echo "- **Image Size**: \`${{ steps.train-image-info.outputs.image-size }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Image URI**: \`${{ steps.train-image-info.outputs.image-uri-latest }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### API Image" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ **Build successful** - Image validated but not pushed to registry" >> $GITHUB_STEP_SUMMARY
          echo "- **Image Size**: \`${{ steps.api-image-info.outputs.image-size }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Image URI**: \`${{ steps.api-image-info.outputs.image-uri-latest }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit SHA**: \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "> **Note**: Images are only pushed to Artifact Registry when merged to \`main\` branch." >> $GITHUB_STEP_SUMMARY

      - name: Build Summary (Main Branch)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          echo "## 🚀 Training and API Images Built and Pushed Successfully" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Training Image Details" >> $GITHUB_STEP_SUMMARY
          echo "- **Image URI (SHA)**: \`${{ steps.train-image-info.outputs.image-uri-sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Image URI (Latest)**: \`${{ steps.train-image-info.outputs.image-uri-latest }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Image Size**: \`${{ steps.train-image-info.outputs.image-size }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### API Image Details" >> $GITHUB_STEP_SUMMARY
          echo "- **Image URI (SHA)**: \`${{ steps.api-image-info.outputs.image-uri-sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Image URI (Latest)**: \`${{ steps.api-image-info.outputs.image-uri-latest }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Image Size**: \`${{ steps.api-image-info.outputs.image-size }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit SHA**: \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Model Tracking" >> $GITHUB_STEP_SUMMARY
          echo "- Models are tracked with **DVC** and automatically pushed to \`gs://vibeops-models\`" >> $GITHUB_STEP_SUMMARY
          echo "- Models are versioned and linked to code/data versions" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Next Steps: Submit Training Job or Deploy API" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Submit Training Job:**" >> $GITHUB_STEP_SUMMARY
          echo "1. Go to **Actions** → **Train Model (Manual)**" >> $GITHUB_STEP_SUMMARY
          echo "2. Click **Run workflow**" >> $GITHUB_STEP_SUMMARY
          echo "3. Select a preset configuration or customize parameters" >> $GITHUB_STEP_SUMMARY
          echo "4. Choose image tag: \`latest\` (recommended) or \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Deploy API:**" >> $GITHUB_STEP_SUMMARY
          echo "- API will be automatically deployed when \`src/mlops_project/api.py\` is updated" >> $GITHUB_STEP_SUMMARY


